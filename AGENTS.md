# AGENTS.md — ORV-System-Mod
> Auto-generated by the dev pipeline scanner. Do not edit manually.
> Last updated: 2026-07-29T18:46:05+02:00
> ⚠️ To update this file, architectural changes must be detected by the scanner and confirmed by a human.

## How to Read This File

Every agent working in this repository must follow these project-specific
rules before changing code.

## Stack Context

NeoForge 21.1.235 for Minecraft 1.21.1, Java 21, Gradle/NeoGradle, Mojang
Codecs, CustomPacketPayload networking, JUnit Jupiter, and Mockito.

## Code Style Rules

- DO keep packages below `com.weeeedddd.orv`.
- DO use records for immutable packets and snapshots.
- DO validate constructor and wire inputs before storing them.
- DO use `long` for coin and energy values.
- DO use translation keys for player-visible command feedback.
- DON'T introduce a second authoritative copy of an existing player value.
- DON'T put server-side mutation logic in screens, packet records, or commands.

## Architecture Guardrails

- `OrvMod` is the composition root; register event listeners there or through
  event subscriber classes.
- Persistent player state belongs in Codec-backed NeoForge attachments.
- All coin mutations go through `CoinService`.
- Server-to-client payload handlers may only publish immutable client
  snapshots or update explicitly client-owned state.
- Packet registration and packet IDs remain centralized under `network`.
- Physical-client code stays under `client` and must not be required by
  dedicated-server execution paths.
- Guild mutations remain server-authorized through `GuildService`.

## Testing Requirements

- Current measured test count: 14; line coverage is not configured.
- Write a failing JUnit test before implementing new behavior.
- Test packet codecs with real buffers rather than mocked codecs.
- Test state objects directly; mock Minecraft runtime types only at boundaries.
- Cover success, boundary, malformed-input, and authorization cases.
- Run focused tests during RED/GREEN and `gradlew clean build` before handoff.
- Do not skip or disable failing tests.

## Modularity Conventions

- Persistent models and attachment registration → `data`
- Server-side feature services → feature package such as `economy`
- Brigadier registration and adapters → `command`
- Payloads, codecs, and handlers → `network`
- Client caches, HUDs, screens, and keybindings → `client`
- Tests mirror the production package below `src/test/java`

## Security Rules (All Agents)

- Never hardcode secrets, tokens, or credentials.
- Treat all serverbound packet values and command input as untrusted.
- Enforce permissions on the server, never only in the client UI.
- Bound all network strings and collections before allocation.
- Never let a clientbound cache become a server-authoritative data source.
- Use checked arithmetic for currencies and resource values.

## Agent-Specific Instructions

### Orchestrator

- Confirm whether a new field is authoritative, derived, or client-only.
- Identify attachment, packet, and lifecycle-event dependencies before work.
- Keep phase tracking in `tasks/` and the connected Linear/GitHub issues.

### Architect

- Prefer one authoritative attachment per domain concept.
- Version or explicitly migrate packet and persistence schema changes.
- Keep future sponsor/stigma data composable with existing player state.

### Developer

- Use Java 21 without preview features.
- Prefer Mojang Codecs and NeoForge APIs over custom NBT plumbing.
- Keep service operations atomic and synchronize only after successful writes.
- Do not directly mutate attachments from Brigadier handlers.

### PR Reviewer

- Treat client-authoritative mutations, unbounded packet input, negative
  currency/resource states, and dedicated-server client linkage as critical.
- Require tests for packet schema changes and attachment migrations.

### QA Agent

- Always test zero, maximum values, overflow, malformed strings, reconnect,
  respawn, dimension change, and permission boundaries.
- For networking, verify both codec round trips and handler thread handoff.
